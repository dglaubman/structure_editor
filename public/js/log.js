// Generated by CoffeeScript 1.6.3
(function() {
  var debug, error, fatal, log, logger, prefix, root, trace, util, verbose, write;

  util = require('util');

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  prefix = "";

  verbose = false;

  debug = false;

  logger = function(argv, pre) {
    debug = argv.d;
    verbose = argv.v;
    if (pre) {
      return prefix = pre;
    }
  };

  write = function(o) {
    return util.puts("" + prefix + o);
  };

  log = function(o) {
    if (verbose) {
      return write(o);
    }
  };

  trace = function(o) {
    if (debug) {
      return write(util.inspect(o, false, null));
    }
  };

  error = function(err) {
    return write(err);
  };

  fatal = function(err) {
    error(err);
    return process.exit(0);
  };

  root.logger = logger;

  root.log = log;

  root.trace = trace;

  root.fatal = fatal;

  root.error = error;

}).call(this);
