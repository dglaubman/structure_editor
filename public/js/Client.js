// Generated by CoffeeScript 1.6.3
(function() {
  var controller, currentGraph, graphClickHandler, log;

  log = new Log(d3.select("#console"));

  log.write("Starting up ...");

  currentGraph = "standard";

  graphClickHandler = function(d) {
    d3.select("#" + currentGraph).classed('selected', false);
    d3.select(this).classed('selected', true);
    return currentGraph = update(this.id);
  };

  d3.select("#clear").on('click', function() {
    return log.clear();
  });

  d3.select("#start").on("click", function(d) {
    return controller.subscribe(currentGraph);
  });

  d3.select("#run_one").on("click", function(d) {
    return controller.run(1);
  });

  d3.select("#run_many").on("click", function(d) {
    return controller.run(1000);
  });

  d3.select("#run_many2").on("click", function(d) {
    return controller.run(10000);
  });

  d3.selectAll(".posgraph").on("click", graphClickHandler);

  d3.select("#direction").on("click", function(d) {
    return toggleDirection();
  });

  d3.select("#node_sep").on("click", function(d) {
    return toggleNodeSeparation();
  });

  d3.select("#verbose").on("click", function(d) {
    return log.toggle();
  });

  controller = new Controller(log);

  controller.start(function() {
    return graphClickHandler.call(document.getElementById(currentGraph));
  });

}).call(this);
